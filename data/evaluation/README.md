# Grader evaluation data

Create a human-labelled benchmark of roughly 30–50 representative HSK1 learner answers. Each item should contain the exercise, target concept, learner answer, scores for every rubric dimension, gate/pass label, error codes, annotator notes, and adjudication status.

Do not commit personally identifiable learner data. `TODO(evaluation)`: define the JSONL schema, annotation guide, inter-annotator process, and acceptance metrics.
